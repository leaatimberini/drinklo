import SearchClient from "./search-client";

export const metadata = {
  title: "Buscar | Storefront",
  description: "Búsqueda instantánea de productos",
};

export default function SearchPage() {
  return <SearchClient />;
}
